# 价格页面引用与弹出清单

> 记录时间：2026-09-13
> 背景：价格页面已拆分为 `src/themes/default/blocks/` 下的四个组件（pricing-promo / pricing-plans / pricing-why-yearly / pricing-compare-table），组装层为 `pricing.tsx`，共享常量在 `pricing-constants.ts`。
> 待办：以下引用点后续可能需要统一处理。

## 一、弹出价格（内嵌 Pricing 组件的弹窗）

- `src/shared/blocks/generator/image.tsx`
  - 唯一直接 import `Pricing` 组件的地方（28 行），在 Dialog 弹窗里内嵌 `<PricingBlock ... hideHeader compact>`（1719 行）
  - 触发点 1：508 行，选「超清」质量但无有效订阅时 `setShowPricingDialog(true)`
  - 触发点 2：885 行，点击生成时 qualityStyle 为 ultra 且无订阅

- `src/shared/blocks/payment/payment-modal.tsx`
  - 全局 PaymentModal（支付方式弹窗，不是价格页），读取全局 `isShowPaymentModal` 渲染
  - 打开它的只有一处：`src/themes/default/blocks/pricing-plans.tsx:536`（handlePayment，`select_payment_enabled` 开启时）
  - `payment-providers.tsx:39` 读取 `setIsShowPaymentModal`，用于选择供应商后关闭

## 二、引用 / 跳转到 /pricing 页面（链接类）

- `src/themes/default/blocks/header.tsx:83` — 顶部公告横幅按钮，默认 `href || '/pricing'`
- `src/shared/blocks/generator/music.tsx:624` — 积分不足时的「购买积分」按钮 `Link href="/pricing"`
- `src/app/[locale]/(landing)/settings/credits/page.tsx:127` — 积分面板「购买」按钮 `url: '/pricing'`
- `src/app/[locale]/(landing)/settings/billing/page.tsx:203、223` — 「调整套餐」「订阅」按钮 `url: '/pricing'`

## 三、页面本体渲染

- `src/app/[locale]/(landing)/pricing/page.tsx`
  - 通过 `DynamicPage` 的 `block: 'pricing'` 走主题动态渲染（`themes/default/blocks/index.tsx:24` 导出），不是直接 import

## 备注

- `video.tsx` 未发现任何价格弹窗或 `/pricing` 引用。
- `image.tsx` 是外部唯一调用 Pricing 组件的调用方，传 `hideHeader + compact + className`，组装层已原样保留这三个 props，无需改动。
- 行号为拆分/分析当时代码的行号，后续代码变动后需重新核对。
